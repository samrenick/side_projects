# weltall - simple snippet manager

## Instructions

cargo build --release  
add snippets to snippets.toml  
call from command line with weltall [snippets location] [language] [keyword]  
e.g. weltall "snippets.toml" "rust" "?"
